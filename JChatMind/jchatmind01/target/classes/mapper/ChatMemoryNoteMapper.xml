<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kaer.mapper.ChatMemoryNoteMapper">

    <resultMap id="BaseResultMap" type="com.kaer.model.entity.ChatMemoryNote">
        <id property="id" column="id" jdbcType="VARCHAR"/>
        <result property="sessionId" column="session_id" jdbcType="VARCHAR"/>
        <result property="content" column="content" jdbcType="VARCHAR"/>
        <result property="createdAt" column="created_at" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,session_id,content,created_at
    </sql>

    <insert id="insert" parameterType="com.kaer.model.entity.ChatMemoryNote"
            keyColumn="id" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO chat_memory_note
            (session_id, content, created_at)
        VALUES (CAST(#{sessionId} AS uuid), #{content}, #{createdAt}) ON CONFLICT (session_id)
    DO
        UPDATE SET
            content = EXCLUDED.content
    </insert>

    <select id="selectBySessionId" resultMap="BaseResultMap">
        SELECT id,
               session_id,
               content,
               created_at
        FROM chat_memory_note
        WHERE session_id = CAST(#{sessionId} AS uuid)
        ORDER BY created_at ASC
    </select>
</mapper>
